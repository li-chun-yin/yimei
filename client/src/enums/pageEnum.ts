export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // // basic home 目前dashboard页面没有开发，先注释了
  // BASE_HOME = '/dashboard',
  BASE_HOME = '/',
  // error page path
  ERROR_PAGE = '/exception',
  // error log page path
  ERROR_LOG_PAGE = '/error-log/list',
}
